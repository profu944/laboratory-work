#define _CRT_SECURE_NO_WARNINGS
#include <math.h>
#include <stdio.h>
#include <locale.h>


main()
{
	float x, y;

	scanf("%f", &x);

	if (x <= -1)
	{
		y = -1 / (x * x);
	}
	else if ((x >= -1) && (x <= 2))
	{
		y = pow(x, 2);
	}
	else
	{
		y = 4;
	}
	printf(" f(x) = %lf", y);
	return 0;
}
